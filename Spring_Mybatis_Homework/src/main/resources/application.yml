spring:
  application:
    name: Spring_Mybatis_Homework
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://127.0.0.1:3306/mybatis_test_db
#    username: root
#    password: mzyudada
# 此处不再需要通过yml配置文件配置数据库连接信息,
#  因为已经在jdbc.properties当中配置
#  并且由@PropertySource("classpath:jdbc.properties")注释读取

logging:
  level:
    SpringBoot:
      Spring_Mybatis_Test:
        Mapper: DEBUG

server:
  port: 1453